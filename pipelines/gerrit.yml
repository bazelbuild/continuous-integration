---
platforms:
  ubuntu2204:
    shell_commands:
      - "sudo apt -y update && sudo apt -y install openssh-client"
    build_targets:
      - "//:release"
      - "//:api"
    test_targets:
      - "//..."
  macos:
    build_targets:
      - "//:release"
      - "//:api"
    test_targets:
      - "//..."
