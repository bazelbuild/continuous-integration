---
platforms:
  fedora39:
    build_targets:
      - "//:release"
      - "//:api"
    test_flags:
      - "--test_tag_filters=-git,-git-protocol-v2,-git-upload-archive,-ssh"
    test_targets:
      - "//..."
  ubuntu2204:
    build_targets:
      - "//:release"
      - "//:api"
    test_targets:
      - "//..."
  macos:
    build_targets:
      - "//:release"
      - "//:api"
    test_flags:
      - "--test_tag_filters=-git,-git-protocol-v2,-git-upload-archive,-ssh"
    test_targets:
      - "//..."
